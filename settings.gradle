pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url(vs_maven_url) }
        maven { url "https://maven.fabricmc.net/" }
        maven { url "https://maven.shedaniel.me/" }
        maven { url "https://files.minecraftforge.net/maven/" }
    }
}

include("common")
include("fabric")
include("forge")
includeBuild("vs-core")

rootProject.name = "valkyrienskies"
